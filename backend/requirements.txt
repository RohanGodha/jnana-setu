# --- Core API ---
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
python-multipart>=0.0.9
sse-starlette>=2.1

# --- Auth ---
python-jose[cryptography]>=3.3
bcrypt>=4.1
email-validator>=2.0   # required by pydantic EmailStr in models.py

# --- Rate limiting ---
slowapi>=0.1.9

# --- RAG orchestration ---
langgraph>=0.2
langchain>=0.2
langchain-core>=0.2
langchain-text-splitters>=0.2
langchain-anthropic>=0.1
anthropic>=0.34
openai>=1.40            # Groq / Gemini / DeepSeek / OpenAI (OpenAI-compatible)

# --- Vector store + retrieval ---
chromadb>=0.5
rank-bm25>=0.2.2
qdrant-client>=1.10   # hosted vector backend

# --- Embeddings + rerank (optional heavy deps; dev fallback exists) ---
sentence-transformers>=3.0

# --- Ingestion parsers ---
PyMuPDF>=1.24
beautifulsoup4>=4.12

# --- Misc ---
python-dotenv>=1.0
httpx>=0.27
